{
  "name": "printer-socket-server",
  "version": "1.0.0",
  "private": true,
  "description": "HTTP POST /printer → broadcast JSON a clientes WebSocket (Socket.IO)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  }
}
